# ActiveMQ Broker Configuration
activemq.broker-url=tcp://localhost:61616
activemq.queue=TEST.QUEUE

# SSL Configuration (Optional)
activemq.keystore=keystore.jks
activemq.keystorePassword=changeit

# Connection Settings (No authentication required)
activemq.clientId=my-jms-client
